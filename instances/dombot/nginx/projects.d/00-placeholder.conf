# Keep this file so `include .../projects.d/*.conf` never matches an empty glob.
# Real per-project routes are added by scripts/project-launch.sh → *.conf here.
location = /__clawvis_projects_placeholder {
    internal;
    return 404;
}
