# MEMORY.md — long-term memory (Clawvis setup)

This file was generated by Clawvis setup. Your Brain lives on disk under the symlinked `memory/` folder in this workspace.

## Projects

- Canonical project notes: see `memory/projects/` (PARA / project slugs).

## Tasks

- Kanban-backed tasks sync under `memory/kanban/` when using Clawvis Kanban.

## Context

- Session context and scratch: use `memory/daily/` or `memory/resources/` as needed.

## Notes

- **Brain root (symlink target):** `{{MEMORY_ROOT_ABS}}`
- Do not commit secrets. Regenerate via Hub **Setup** or `clawvis start` sync if paths change.
