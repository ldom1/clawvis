# LocalBrain (Clawvis fallback template)

> Fallback when GitHub raw fetch is unavailable. Replace `{{MEMORY_ROOT_ABS}}` with your instance memory path.

## Brain root

{{MEMORY_ROOT_ABS}}

Use this path for Brain notes, projects, and daily logs.
