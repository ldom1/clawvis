# Brain Maintenance (core)

L1/L2 maintenance: trim (audit), recalibrate (drift), recover (context).

```bash
uv run python -m brain_maintenance trim
uv run python -m brain_maintenance recalibrate
uv run python -m brain_maintenance recover
```

Logging: dombot_logger (~/.openclaw/skills/logger/core).
