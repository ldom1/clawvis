[project]
name = "clawvis-scheduler"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "hub-core",
  "aiohttp>=3.9",
  "apscheduler==3.10.4",
  "httpx>=0.28",
  "pydantic>=2.12.5",
  "pyyaml==6.0.2",
]

[dependency-groups]
dev = []

[tool.uv.sources]
hub-core = { workspace = true }
